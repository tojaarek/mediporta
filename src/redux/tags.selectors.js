export const selectOrder = (state) => state.tags.order;
export const selectSort = (state) => state.tags.sort;
export const selectPageSize = (state) => state.tags.pageSize;
export const selectPage = (state) => state.tags.page;
export const selectTotalResults = (state) => state.tags.totalResults;
export const selectTags = (state) => state.tags.tags;
export const selectIsLoading = (state) => state.tags.isLoading;
